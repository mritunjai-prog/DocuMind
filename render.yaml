services:
  - type: web
    name: documind-backend
    env: docker
    rootDir: .
    dockerfilePath: backend/Dockerfile
    healthCheckPath: /health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
